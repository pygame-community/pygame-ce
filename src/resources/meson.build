data_files = files(
    'freesansbold.ttf',
    'pygame_icon.bmp',
)
if plat == 'mac'
    data_files += 'pygame_icon_mac.bmp'
endif

install_data(data_files, install_dir: pg_dir, install_tag: 'pg-tag')
